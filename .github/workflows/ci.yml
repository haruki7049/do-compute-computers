name: Ziglang CI

on:
  push:
  pull_request:

jobs:
  run-zig-build-test:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v7.0.1
      - uses: mlugg/setup-zig@v2.2.1
        with:
          version: 0.16.0
      - run: zig build test
