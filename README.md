# do-compute-computers

```sh
zig build # You haven't to install pkg-config, alsa-lib,libpulseaudio, and libpipewire
zig build play # You must install pkg-config, alsa-lib, libpulseaudio, and libpipewire
```
